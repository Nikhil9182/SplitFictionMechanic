using Unity.Cinemachine;
using UnityEngine;

public class PlayerCinemachineCameraInputHandler : MonoBehaviour, AxisState.IInputAxisProvider
{
    public float GetAxisValue(int axis)
    {
        throw new System.NotImplementedException();
    }
}
